function findLogestWordLength(str)
{
  let wordArr = str.split(" ");
  let maxlen = 0;

  for(let i = 0; i < wordArr.length; i++)
  {
    let word = wordArr[i];
     maxlen = Math.max(wordArr.length, max)
  }


}

console.log(findLogestWordLength("Srinivas is an  dev engineer"));